{
  "name": "geo-json-statistics-server",
  "version": "1.0.0",
  "description": "Express API server for geo-json-statistics",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "start": "npx tsx --env-file=.env --watch  src/index.ts",
    "dev": "nodemon index.js",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.5"
  }
}
